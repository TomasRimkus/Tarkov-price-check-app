<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels1="clr-namespace:Tarkov_price_check_app.ViewModels"
             x:Class="Tarkov_price_check_app.Views.HideoutPage">
    <ContentPage.BindingContext>
        <viewModels1:HideoutViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="" BackgroundColor="Transparent"></Label>
            <Label Text="{Binding SetStatus}"
                   VerticalOptions="CenterAndExpand"
                   HorizontalTextAlignment="Center"
                   FontAttributes="Bold"
                   />

            <Button IsEnabled="{Binding SetButtonStatus}" Command="{Binding RefreshCommand}" Text="Refresh prices" />

            <StackLayout>

                <Label Text="Intelligence center" HorizontalTextAlignment="Center" FontAttributes="Bold" BackgroundColor="LightGray"></Label>

                <ListView  
                    ItemsSource="{Binding IntelDataCollection}"
                    IsPullToRefreshEnabled="False"  
                    HasUnevenRows="true" 
                    SeparatorVisibility="Default">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Frame>
                                    <StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="Item name"/>
                                            <Label Text="{Binding ResultItemName}"/>
                                        </StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="Current profit"/>
                                                <Label Text="{Binding ResultProfit}"/>
                                        </StackLayout>
                                    </StackLayout>
                                </Frame>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

                <Label Text="Lavatory" HorizontalTextAlignment="Center" FontAttributes="Bold" BackgroundColor="LightGray"></Label>

                <ListView  
                     ItemsSource="{Binding LavDataCollection}"
                     IsPullToRefreshEnabled="False"  
                     HasUnevenRows="true" 
                     SeparatorVisibility="Default">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Frame>
                                    <StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="Item name"/>
                                            <Label Text="{Binding ResultItemName}"/>
                                        </StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                                <Label Text="Current profit"/>
                                                <Label Text="{Binding ResultProfit}"/>
                                        </StackLayout>
                                    </StackLayout>
                                </Frame>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

                <Label Text="Workbench" HorizontalTextAlignment="Center" FontAttributes="Bold" BackgroundColor="LightGray"></Label>

                <ListView  
                     ItemsSource="{Binding WorkDataCollection}"
                     IsPullToRefreshEnabled="False"  
                     HasUnevenRows="true" 
                     SeparatorVisibility="Default">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Frame>
                                    <StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                            <Label Text="Item name"/>
                                            <Label Text="{Binding ResultItemName}"/>
                                        </StackLayout>
                                        <StackLayout Orientation="Horizontal">
                                                <Label Text="Current profit"/>
                                                <Label Text="{Binding ResultProfit}"/>
                                        </StackLayout>
                                    </StackLayout>
                                </Frame>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>